# URL pública do backend (Render, Railway, etc.), incluindo /api no final.
# Em produção, configure esta variável no painel da Vercel (Settings > Environment Variables).
VITE_API_URL=http://localhost:3333/api
